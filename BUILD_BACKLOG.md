# Build Backlog — Perlego Onboarding (ICP 1: Exam in 2 Days)

> هدف: ساخت prototype کلیک‌پذیر و desktop-first در Lovable با **کمترین مصرف credit**.
> این فایل «نقشه‌ی ساخت» است. پرامپت‌های واقعی در `LOVABLE_PROMPTS.md` می‌آیند.

---

## ۱. قوانین صرفه‌جویی در credit (قبل از هر کاری بخوان)

| قانون | چرا |
|---|---|
| **هر پیام به Lovable = یک credit.** پس پرامپت‌های کم و متراکم، نه بیست تسک ریز. | مستقیم‌ترین اهرم صرفه‌جویی |
| **design system را در همان پرامپت اول کامل تزریق کن.** | بعد از آن هیچ پرامپتی لازم نیست رنگ/فونت/spacing را توصیف کند. تخمیناً بزرگ‌ترین صرفه‌جویی کل پروژه. |
| **هیچ بک‌اند واقعی نساز.** بدون Supabase، بدون auth واقعی، بدون DB. | brief اجازه داده همه simulate شوند. اتصال بک‌اند پرمصرف‌ترین کار در Lovable است. |
| **همه‌ی داده در یک فایل mock واحد.** | تغییر محتوا بدون خرج credit، دستی در ادیتور. |
| **اشکالات را جمع کن، یکجا بفرست.** سه اصلاح در یک پیام = ۱ credit، نه ۳ تا. | |
| **هرگز نگو «قشنگ‌ترش کن» یا «بهترش کن».** | مبهم = چند دور رفت‌وبرگشت = credit سوخته. همیشه دقیق بگو چه چیزی کجا چطور. |
| **اگر پلن Lovable ات chat/plan mode دارد، برنامه‌ریزی را آنجا انجام بده.** | build mode را فقط برای ساخت واقعی نگه دار. |
| **قبل از هر پرامپت اصلاحی، اسکرین‌شات را برای من بفرست.** | جلوی حدس‌زدن کور و پرامپت هدررفته را می‌گیرد. |

**بودجه‌ی هدف: ۶ credit برای هسته + ۲ تا ۳ برای اصلاحات = ~۹ credit کل.**

---

## ۲. تصمیم‌های معماری (که credit را کم می‌کنند)

### ۲.۱ همه چیز mock است
| قابلیت | چطور simulate می‌شود |
|---|---|
| Smart Search / تطبیق موضوع | نگاشت ثابت در فایل mock — بدون هیچ فراخوانی واقعی |
| Research Assistant | پاسخ‌های از پیش نوشته‌شده با citation |
| Authentication | مودال ساده، فقط state محلی |
| Payment | مودال ساده، فقط state محلی |
| Book database | آرایه‌ی ثابت در فایل mock |

### ۲.۲ ترفند «هوشمند به‌نظر رسیدن» بدون خرج credit
نقشه‌ی نتایج **همیشه همان مجموعه‌ی کیوریت‌شده** است، اما **کلمات خودِ کاربر** روی کارت‌ها به‌عنوان تگ‌های منطبق echo می‌شوند. ارزان، ساده، و در دمو کاملاً باورپذیر.

**بیمه‌ی دمو:** در صفحه‌ی ورود، ۳ چیپ موضوع آماده بگذار («یکی از این‌ها را امتحان کن»). این هم‌زمان:
- یک تصمیم UX درست است (ضد blank-canvas — الگوی Instagram/Icons8)
- تضمین می‌کند دمو همیشه نتیجه‌ی باکیفیت بدهد

### ۲.۳ دامنه‌ی محتوای نمونه
**روان‌شناسی شناختی — حافظه (Memory & Cognition).**
دلیل: برای داور بلافاصله قابل‌فهم است، شکل «امتحانی» دارد (encoding، working memory، منحنی فراموشی)، و نوشتن excerpt باورپذیر برایش آسان است.

### ۲.۴ مدل داده (در پرامپت T0 قفل می‌شود تا بعداً بازسازی لازم نباشد)

```ts
type Chapter = {
  id: string
  order: number            // ترتیب در نقشه
  bookTitle: string
  bookAuthor: string
  chapterTitle: string
  matchedTopics: string[]  // با ورودی کاربر جایگزین می‌شود
  readMinutes: number
  excerpt: string          // پاراگراف اثبات (P0-3)
  pageRef: string          // مثلا "Ch. 4, p. 112"
  spineColor: string       // از accent tokens
  isFree: boolean          // فقط order === 1
  body: string[]           // پاراگراف‌های متن کامل برای reader
}
```

---

## ۳. جریان صفحه‌ها

```
/            صفحه‌ی ورود — «موضوعات امتحانت رو paste کن»
   ↓ (حالت «در حال خواندن موضوعات تو» — ۲ ثانیه)
/map         Revision Map — ۵ کارت فصل، مرتب، با excerpt اثبات
   ↓ (کلیک روی فصل ۱ — رایگان، بدون حساب)
/read/:id    Reader — متن + هایلایت + پنل Research Assistant
   ↓ (تلاش برای هایلایت/ذخیره)  → مودال signup
   ↓ (تلاش برای فصل ۲)          → مودال paywall با فریم loss
   ↓ (پرداخت)                   → مستقیم برگرد به همان reader ⚠️ بدون صفحه‌ی upsell
```

⚠️ **درس WSJ:** بعد از پرداخت هیچ صفحه‌ی آفر/newsletter/upsell نداریم. مستقیم به مطالعه.

---

## ۴. بک‌لاگ تسک‌ها

### 🔴 T0 — پایه (۱ credit) — *پرامپت ساخت پروژه*
**شامل:** design system کامل (محتوای `tokens.css` عیناً) + مسیرها + فایل mock data + shell صفحه (TopNav مینیمال).
**چرا اول:** بعد از این، هیچ پرامپتی نیاز به توصیف استایل ندارد.
**Acceptance:**
- [ ] توکن‌های CSS موجود‌اند و فونت‌ها لود می‌شوند (Playfair Display به‌جای Geller)
- [ ] ۳ مسیر وجود دارند (`/`, `/map`, `/read/:id`) حتی اگر خالی
- [ ] فایل `mockData.ts` با ۵ فصل کامل ساخته شده
- [ ] پس‌زمینه سفید/paper، خاکستری‌های گرم، بدون shadow مشکی تخت

---

### 🔴 T1 — صفحه‌ی ورود (۱ credit) — **P0-1**
یک textarea. بدون nav شلوغ، بدون pitch مارکتینگی، بدون signup.
**Acceptance:**
- [ ] عنوان با فونت display؛ یک textarea بزرگ
- [ ] ۳ چیپ موضوع آماده که با کلیک، textarea را پر می‌کنند
- [ ] یک دکمه‌ی primary (`#3327EC`)
- [ ] **هیچ‌جا عدد «۱.۴ میلیون کتاب» نیست** (تصمیم حذف صریح)
- [ ] هیچ لینک signup/login برجسته‌ای نیست

---

### 🔴 T2 — Revision Map + حالت انتقال (۱ credit) — **P0-2 + P0-3 + P2-7**
**Acceptance:**
- [ ] حالت «در حال تحلیل» با متن‌های متوالی (~۲ ثانیه) بعد submit
- [ ] ۵ کارت فصل، مرتب‌شده، با شماره‌ی ترتیب («۱ — اول این را بخوان»)
- [ ] هر کارت: عنوان فصل، کتاب/نویسنده، **تگ‌های موضوع echo‌شده از ورودی کاربر**، زمان مطالعه
- [ ] **excerpt اثبات روی هر کارت قابل‌مشاهده** + ارجاع صفحه
- [ ] نوار زمانی آرام بالای نقشه: «۲ روز وقت داری — این ۵ فصل ۷۸ دقیقه است» ⚠️ **بدون countdown**
- [ ] فصل ۱ نشان «رایگان، بدون حساب» دارد

---

### 🟠 T3 — Reader + Research Assistant (۱ credit) — **P1-4 (بخش خواندن) + P2-6**
**Acceptance:**
- [ ] ستون مطالعه با عرض حداکثر ۶۴۰px، پس‌زمینه‌ی paper
- [ ] متن فصل ۱ کامل و خوانا
- [ ] انتخاب متن → دکمه‌ی شناور هایلایت
- [ ] پنل کناری Research Assistant با **سوالات از پیش پرشده از موضوعات خود کاربر**
- [ ] پاسخ‌ها با citation («Ch. 4, p. 112») نمایش داده می‌شوند

---

### 🟠 T4 — لحظات تبدیل (۱ credit) — **P1-4 (بخش تبدیل)**
**Acceptance:**
- [ ] هایلایت/ذخیره → مودال signup (FITD): «۳ هایلایت کردی. برای فردا نگه‌شان داریم؟»
- [ ] کلیک روی فصل ۲ → مودال paywall با **فریم loss**: «۳ هایلایتت + ۴ فصل باقی‌مانده‌ی نقشه‌ات»
- [ ] بعد از پرداخت: **مستقیم به reader**، هیچ صفحه‌ی upsell
- [ ] هر دو مودال کاملاً mock (فقط state محلی)

---

### 🟡 T5 — پاس اصلاح (۱–۳ credit)
اشکالات را جمع کن و **یکجا** بفرست. اینجا چیز جدیدی نمی‌سازیم.

---

### 🟢 اگر credit ماند (به ترتیب اولویت)
1. **P3-9** پیشرفت روی نقشه (۲ از ۵ فصل خوانده شد) — goal gradient قابل‌مشاهده
2. **P2-8** قلاب بازگشت («از جایی که موندی ادامه بده»)
3. **P3-10** «مرتبط نیست؟» → بازچینش نقشه

---

## ۵. چیزهایی که **هرگز** از Lovable نخواه (تله‌های credit)

- ❌ اتصال Supabase یا هر بک‌اند واقعی
- ❌ auth یا payment واقعی
- ❌ جستجوی واقعی یا فراخوانی API هوش مصنوعی
- ❌ نسخه‌ی موبایل (brief گفته desktop-first — موبایل یعنی دو برابر کار)
- ❌ انیمیشن‌های پیچیده (design system گفته motion آرام، ۱۲۰–۱۶۰ms)
- ❌ صفحه‌ی مارکتینگ / لندینگ (عمداً حذف شده)
- ❌ «قشنگ‌ترش کن» یا هر درخواست مبهم دیگر

---

## ۵.۵ وضعیت فعلی ساخت (به‌روزرسانی زنده)

| تسک | وضعیت | یادداشت |
|---|---|---|
| T0 — پایه | ✅ انجام شد | routes + SessionProvider |
| push design system + mockData | ✅ انجام شد | صفر credit، از طریق git |
| T1 — صفحه‌ی ورود | ✅ انجام شد | بدون ایراد |
| T2 — Revision Map | ✅ انجام شد | + T2-fix (تگ‌های تکراری) رفع شد |
| T3 — Reader + Assistant | ✅ انجام شد | بدون ایراد |
| T4 — لحظات تبدیل | ✅ انجام شد | + T4-fix (مودال Save تکراری) رفع شد |
| مسیر کامل end-to-end | ✅ تست و تأیید شد | توسط کاربر |

**credit مصرف‌شده تخمینی: ۷ (T0, T1, T2, T2-fix, T3, T4, T4-fix)** — در بودجه.

**باقی‌مانده:** موارد 🟢 بخش ۴ (پیشرفت روی نقشه، قلاب بازگشت، «مرتبط نیست؟») اختیاری‌اند. تصمیم بعدی: پولیش بیشتر یا رفتن سراغ دمو/submission.

---

## ۶. چک‌لیست آمادگی دمو (بعد از T4)

مسیر ۲ دقیقه‌ای که داور باید تجربه کند:
1. صفحه‌ی ورود → کلیک روی یک چیپ موضوع
2. حالت تحلیل → Revision Map
3. اشاره به excerpt اثبات روی کارت‌ها (**اینجا لحظه‌ی aha است**)
4. باز کردن فصل ۱ — بدون حساب، در حال خواندن (**activation زیر ۶۰ ثانیه**)
5. هایلایت → مودال signup
6. تلاش برای فصل ۲ → paywall با فریم loss
7. پرداخت → مستقیم برگشت به مطالعه
